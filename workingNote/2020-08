7.27
1.区分是否中文
escape(str)包含%u

8.5
1.excel提取文档名称
公式——定义名称（XXX,=GET.WORKBOOK(1)）——=INDEX(XXX,ROW(A1))——下拉

2.=INFO("numfile")或定义名称（XXX，=GET.WORKBOOK(4)）——XXX

8.13
1.多行注释的正则（忽略注释）
'aa/*aaa*/a'.replace(/\/\*(.|\r\n)*\*\//g,'')

2.单行注释的正则（忽略注释）
'aa/*aaa*/a'.replace(/\/\/.*\n/g,'')

8.31
1.正则截取字符串（不包含开始和结束字符串）
/(?<=截取开始字符串)(.*?)(?=截取结束字符串)/
